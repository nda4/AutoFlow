#ifndef BUTTON_H
#define BUTTON_H

#include <windows.h>

// This function creates a button in the given window (hwnd) and returns the handle to the button.
HWND CreateButton(HWND hwnd);

#endif // BUTTON_H
