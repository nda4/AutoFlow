#ifndef DROPDOWN_H
#define DROPDOWN_H

#include <windows.h>

// This function creates a dropdown list in the given window (hwnd) and returns the handle to the dropdown list.
HWND CreateDropdown(HWND hwnd);

#endif // DROPDOWN_H
